import { Sidebar } from "@/components/ui/sidebar"

/**
 * Renders the application sidebar using the "inset" Sidebar variant.
 *
 * @returns A JSX element for the Sidebar configured with variant "inset".
 */
export function AppSidebar() {
  return <Sidebar variant="inset"></Sidebar>
}